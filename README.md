# Tetoris

A Tetris game where tiles drop at the pace of music.

recommended to use mouse for moving tiles instead of WASD

setup & run:
```
uv run tetris
```

To add more music drop the mp3 files in `resources/music`
